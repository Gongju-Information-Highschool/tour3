# 공주 지도 탐색 서비스 "맵으로 공주" — 최종 개발기획안 (v2.1)

> **대상**: 고등학교 경영정보 전공 학생 (개발 입문자)
> **포지션**: 『AI 관광추천 v2.4』의 **자매 프로젝트** — 같은 지역(공주시 30곳), 완전히 다른 UI·데이터·알고리즘
> **원칙**: 내부 DB(SQLite) + 로컬호스팅 + 단일 폴더. 단, **지도 UI를 위해 Leaflet 1개만 예외 허용**
> **데이터**: **네이버플레이스 사전 수집 CSV** (별점·방문자리뷰수·블로그리뷰수·카테고리·좌표) — 수업 중 API 호출 없음
> **확정 환경**: Python 3.13 (Windows) · Flask · SQLite · Leaflet 1.9(로컬 번들)
> **작성일**: 2026-08-30 · v1.0의 **미결·모순 18건**을 확정으로 바꾼 판 (변경 이력 §15)
> **v2.1**: 코드를 실제로 구현하며 드러난 **6건**을 반영 (변경 이력 §17) ·
> 현재 구현 상태와 검증 결과는 §16

---

## 0. 먼저, v1.0에서 반드시 고쳐야 했던 5건

기획안 v1.0은 방향이 옳다. 다만 **그대로 구현하면 수업 데모가 실패하는 지점**이 다섯 군데 있었다.
나머지 13건은 §15 이력표에 있고, 여기 다섯은 이 프로젝트의 성패를 직접 가른다.

| # | v1.0의 문제 | 왜 치명적인가 | v2.0 확정 |
|---|---|---|---|
| **1** | `final = 0.5 × (보정별점 ÷ 5) + …` | 실데이터 별점은 4.0~4.7 구간에 뭉쳐 있다. ÷5 하면 0.80~0.94, 변별폭 **0.14**. 거리 항의 변별폭(0~1)에 완패한다. **이 수업의 심장인 보정 별점이 최종 순위에 거의 영향을 못 준다.** | **min-max 정규화**로 별점 항을 0~1로 펴서 제1 요인으로 복원 (§5.2) |
| **2** | 취향 부스트 = 카테고리 관심도(0~1) | 6분류 균등 상태의 관심도는 1/6 = **0.167**. 다른 두 항이 0.5 근처에서 노는 동안 부스트만 바닥에 깔린다 | 부스트 = 관심도 ÷ (관심도 + 1/6) → **균등 = 0.5 중립** (§5.4) |
| **3** | 거리 점수의 **기준점이 "지도 중심"** | 지도 중심을 읽으려면 JS가 AJAX로 서버에 보내야 한다. "JS는 지도 초기화 1블록만"이라는 원칙과 정면 충돌 | 기준점 = **폼 select로 고른 장소**(기본 공산성), 패널을 열면 세션에 기록 (§5.3) |
| **4** | 오프라인 시 SVG로 **"자동 폴백"** | 타일 로딩 실패 감지도 JS다. 게다가 `static/fallback_map.svg`는 정적 파일이라 **점수·필터가 반영되지 않는다** | 서버가 기동 시 타일 서버를 1회 확인해 `OFFLINE` 플래그로 템플릿 분기 + **인라인 SVG** 렌더 (§6.4) |
| **5** | 동선의 도보 시간 = 하버사인 × 1.3 ÷ **4km/h 고정** | 공주 30곳은 반경 30km에 흩어져 있다. 공산성→마곡사 14.7km면 화면에 **"도보 286분"**이 찍힌다. 학생이 결과를 믿지 않는 순간 수업이 끝난다 | 구간 3km 초과 시 **차량 40km/h로 전환 + 배지 표시** (§5.6) |

1·2번은 "알고리즘이 조용히 무력화되는" 유형이라 눈에 띄지 않는다.
**동작은 하는데 가르치려던 것이 화면에 안 나타나는 실패**가 가장 위험하다.

---

## 0-1. v2.4와 무엇이 다른가 (설계 선언)

| 축 | v2.4 (AI 관광추천) | **v2.0 (맵으로 공주)** |
|---|---|---|
| 첫 화면 | 취향 입력 폼 | **지도 그 자체** — 마커 30개가 곧 홈 화면 |
| 사용자 행동 | 폼 제출 1회 → 결과 | **탐색 루프** — 필터 → 마커 클릭 → 찜 → 동선 |
| 취향 파악 | 명시적 입력 (테마 0~5점) | **암묵 피드백** — 열람·찜 로그로 자동 학습 |
| 핵심 데이터 | 교사가 설계한 테마·소요시간 | **네이버플레이스 실측치** — 별점·리뷰수·카테고리 |
| 핵심 알고리즘 | 코사인 유사도 + 협업필터링 | **베이지안 보정 별점 + 거리 + 취향 부스트** |
| 산출물 | 3~5곳 추천 코스 | **찜 목록 → 최근접 이웃 동선 정렬** |
| 손계산 학습 목표 | 코사인 유사도 1건 | **보정 별점 1건** ("리뷰 2개 5.0 vs 리뷰 312개 4.4, 누가 이기나") |
| JavaScript | 0줄 | **Leaflet 초기화 1블록** — 학생이 직접 작성하지 않고 프롬프트로 생성 |
| 결측 데이터 | 정제 단계에서 보정 | **알고리즘이 흡수**(R=C) — 결측 처리 자체가 교육 내용 |

두 프로젝트를 나란히 놓으면 학생은 **"같은 문제도 데이터와 UI를 바꾸면 전혀 다른 서비스가 된다"**는
서비스 기획의 본질을 체감한다. v2.4를 마친 반의 2번째 프로젝트로도, 독립 수업으로도 쓸 수 있다.

---

## 1. 프로젝트 한 줄 정의

지도를 열고 → 마커를 눌러 네이버플레이스 정보를 보고 → 마음에 드는 곳을 찜하면,
**보정 별점·거리·내 클릭 패턴**으로 장소를 다시 줄 세워 주고, 찜 목록을 **걷기 좋은 순서의 동선**으로 정렬해 주는 웹 서비스.

이 프로젝트가 학생에게 남겨야 하는 것은 "돌아가는 지도"가 아니라
**"리뷰 수가 다른 별점을 공정하게 비교하는 방법(베이지안 보정)을 손으로 계산해 설명할 수 있는 상태"** 다.
§5.2의 정규화도, §6.4의 오프라인 폴백도, 전부 이 한 문장에 종속된다.

---

## 2. 기술 스택

| 구분 | 선택 | 이유 |
|---|---|---|
| 언어 | Python 3.10+ (검증 3.13) | 학생들이 배운 언어 |
| 웹 | Flask + Jinja2 | 파일 1개로 서버 |
| **지도** | **Leaflet 1.9 (`static/leaflet/` 로컬 번들)** | 무료·오픈소스·API 키 불필요 |
| 지도 타일 | OpenStreetMap (온라인) → **오프라인 판정 시 인라인 SVG** | 학교망 차단 대비 (§6.4) |
| JavaScript | **`map.html`의 초기화 1블록만** | v2.4의 "JS 금지"를 1개 예외로 완화 |
| DB | SQLite (`data/place.db`) | 설치 불필요 |
| 데이터 전처리 | pandas (표준 `csv` 폴백 병행) | pandas 설치 실패해도 수업이 안 멈춤 |
| 그래프 | CSS 막대 (`div` 너비 %) | 차트 라이브러리 없음 |
| 실행 | `python app.py` → 127.0.0.1:5000 | 로컬호스팅 |
| AI 협업 | Claude Code | 프롬프트로 코드 생성·수정 |

```bash
pip install flask pandas        # 설치는 이 한 줄. leaflet은 폴더에 이미 들어 있다
```

> 로그인·회원가입·네이버 지도 API·클라우드는 **범위 밖**. 사용자 식별은 v2.4와 동일하게 Flask session(§6.2).
> Leaflet을 CDN이 아니라 로컬 번들로 두는 이유는 단 하나다 — **학교망이 CDN을 막아도 수업이 굴러가야 한다.**

---

## 3. 폴더 구조 (확정)

```
map-gongju/
├── app.py                    # 서버 + 라우팅 7개
├── scoring.py                # 보정 별점·정규화·거리·취향 부스트 (핵심 학습 파일)
├── route.py                  # 최근접 이웃 동선 + 이동시간
├── geo.py                    # 하버사인·SVG 좌표 변환 (v2.4에서 이식)
├── db.py                     # 조회 함수 모음
├── requirements.txt
├── README.md                 # 실행 3줄 + 파일 지도
├── 프롬프트_대본.md            # 학생 배포용 (§9 전문)
├── data/
│   ├── place.db
│   ├── places_raw.csv        # 네이버플레이스 수집 원본 (33행, 실제로 지저분함)
│   ├── places_clean.csv      # 정제 결과 (30행)
│   ├── category_map.csv      # 네이버 카테고리 원문 → 표준 6분류 매핑표
│   ├── spots_geo.csv         # v2.4에서 이식한 실좌표 30곳
│   ├── params.json           # ★ m·C·손계산 예제값 단일 출처 (§4.5)
│   └── _expected_places.csv  # 교사용 정답 (배포 키트에서 제외)
├── scripts/
│   ├── collect_guide.md      # 교사용: 네이버플레이스 수집 절차 문서 (A2)
│   ├── console_utf8.py       # ★ 윈도우 콘솔 한글 출력 보호 (§17-1)
│   ├── make_placeholder_seed.py  # A2 이전에 쓰는 임시 시드 생성기 (§16)
│   ├── preprocess.py         # 정제 (콤마·중복·결측 좌표·카테고리 매핑)
│   ├── init_db.py            # --reset 분기 (v2.4 규칙 계승)
│   ├── verify.py             # 수업 전날 자동 점검 (§10)
│   ├── check_prompts.py      # 프롬프트 대본 ↔ 코드 정합성 검사
│   ├── make_snapshots.py     # Step별 완성본 생성
│   └── make_dist.py          # 배포본 생성 (_expected_*·snapshots 제외)
├── templates/
│   ├── base.html
│   ├── _svgmap.html          # ★ SVG 지도 매크로 (map/route/offline 공용)
│   ├── map.html              # ★ 홈 = 지도 (Leaflet 1블록은 여기에만)
│   ├── panel.html            # 장소 상세 — 독립 페이지 (부분 템플릿 아님, §15-9)
│   ├── favorites.html        # 찜 목록 + [동선 짜기]
│   ├── route.html            # 동선 결과 (순번 + 구간 이동시간)
│   ├── stats.html            # 보정 전/후 랭킹 비교 대시보드
│   └── offline.html          # 인라인 SVG 산점도
├── snapshots/                # step3_done ~ step7_done (뒤처진 학생용)
└── static/
    ├── leaflet/              # leaflet.js·css·마커 이미지 (오프라인 번들)
    └── style.css
```

> **`_svgmap.html` 은 v2.0에 없던 파일이다.** 지도 그림을 홈·동선·오프라인 세 화면이
> 함께 쓰는데, 같은 SVG 코드를 세 번 적으면 한 곳만 고치는 사고가 난다.
> Jinja 매크로로 묶고 `{% import ... with context %}` 로 불러 쓴다(§17-4).

> v1.0의 `static/fallback_map.svg`는 **삭제**했다. 정적 파일은 점수·필터를 반영할 수 없어,
> 온라인일 때와 오프라인일 때 학생이 전혀 다른 정보를 보게 된다(§15-4).

---

## 4. 데이터 설계 — 네이버플레이스가 원천

### 4.1 수집 항목 (교사 사전 수집, `collect_guide.md`)

지역검색 API는 별점·리뷰수를 주지 않는다. 따라서 두 출처를 합친다.

| 출처 | 가져오는 값 |
|---|---|
| 네이버 **지역검색 결과** | 장소명, 도로명주소, `category_raw`, `mapx`/`mapy` |
| 네이버 **플레이스 페이지** | 별점, 방문자리뷰 수, 블로그리뷰 수, (확장 1용) 영업시간 |

좌표는 **경도 = mapx ÷ 10^7, 위도 = mapy ÷ 10^7**. v2.4 `data/spots_geo.csv`에 이미 30곳 좌표가 있으므로
**좌표는 재수집하지 않고 그대로 이식한다**(교사 작업 약 1h 절약, §8 A2).

수집일을 CSV 모든 행에 기록한다. 리뷰 수는 살아 있는 숫자다 —
**"2026-08 기준 스냅샷"**임을 수업 첫 시간에 명시하고, 이를 데이터 시의성 교육 소재로 쓴다.

### 4.2 테이블 5개 (확정 스키마)

```sql
CREATE TABLE places (
  id            INTEGER PRIMARY KEY,
  name          TEXT NOT NULL,
  category_raw  TEXT NOT NULL,    -- 네이버 원문. 예: '사찰', '베이커리', '박물관'
  category      TEXT NOT NULL,    -- 표준 6분류
  road_address  TEXT,
  lat           REAL NOT NULL,    -- mapy / 10^7
  lon           REAL NOT NULL,    -- mapx / 10^7
  star          REAL,             -- 네이버 별점 (NULL 허용 — 실제로 흔함)
  visitor_rev   INTEGER NOT NULL,
  blog_rev      INTEGER NOT NULL,
  adj_star      REAL NOT NULL,    -- ★ 보정 별점 (적재 시 1회 계산해 저장)
  collected_at  TEXT NOT NULL
);

CREATE TABLE users (              -- session 기반, 회원가입 없음
  id INTEGER PRIMARY KEY, nickname TEXT, created_at TEXT
);

CREATE TABLE clicks (             -- ★ 암묵 피드백 로그
  id INTEGER PRIMARY KEY, user_id INTEGER, place_id INTEGER,
  action TEXT,                    -- 'view' | 'fav' | 'unfav'
  created_at TEXT
);

CREATE TABLE favorites (
  id INTEGER PRIMARY KEY, user_id INTEGER, place_id INTEGER, created_at TEXT,
  UNIQUE(user_id, place_id)       -- ★ 중복 찜 원천 차단
);

CREATE TABLE meta (               -- ★ 신규: 알고리즘 파라미터의 DB 측 사본
  key TEXT PRIMARY KEY, value TEXT
);
-- meta 키: m_review=30, c_mean=4.30, adj_min, adj_max, collected_at, seed_rows
```

`adj_star`를 **적재 시점에 계산해 컬럼으로 저장**하는 것이 v1.0과 다른 점이다.
매 요청마다 30곳을 다시 계산해도 성능은 문제없지만, **정규화에 쓸 `adj_min`/`adj_max`가
요청마다 흔들리면 안 되기 때문**이다(§5.2). 값이 고정되어야 `/stats`의 랭킹 비교도 재현된다.

### 4.3 표준 카테고리 6분류 — 순서 고정

```
역사문화(0) · 자연(1) · 체험(2) · 먹거리(3) · 카페감성(4) · 휴식(5)
```

네이버 원문은 '사찰', '문화유적', '베이커리', '박물관'처럼 제각각이다.
`category_map.csv`가 원문→표준 매핑을 담당하며, **매핑표를 채우는 것 자체가 Step 2 실습**이다.

**목표 분포 (한 카테고리 최소 4곳)**

| 카테고리 | 목표 | 부록 A 초안 배정 | 판정 |
|---|---|---|---|
| 역사문화 | 6~8 | **11** | ⚠ 과다 |
| 자연 | 4~6 | 5 | ✔ |
| 체험 | 4~6 | 5 | ✔ |
| 먹거리 | 4~6 | 4 | ✔ |
| 카페감성 | 4~6 | **2** | ⚠ 부족 |
| 휴식 | 4~6 | **3** | ⚠ 부족 |

**v2.4 부록 A를 그대로 쓰면 필터 칩 6개 중 2개가 거의 빈 화면이 된다.**
카테고리 필터는 이 서비스의 첫 상호작용인데, 눌러 봤더니 마커 2개면 학생은 기능이 고장 났다고 생각한다.
→ 교사 작업 A2에서 **공주시 카페·디저트 3곳, 공원·쉼터 1곳을 네이버플레이스에서 실재 확인해 추가하고,
역사문화 중 성격이 겹치는 3곳을 제외**해 30곳을 재구성한다.
장소 실명은 **반드시 네이버플레이스에서 확인한 것만** 넣는다 — 지어낸 장소가 한 곳이라도 들어가면
"실데이터로 배운다"는 이 프로젝트의 전제가 무너진다.

**매핑은 장소 단위가 아니라 원문 단위다 (구현하며 확인, §17-2)**
부록 A는 장소마다 분류를 배정한 초안이지만, `category_map.csv` 는 `원문 → 표준` 표다.
따라서 **원문이 같으면 분류도 반드시 같아진다.** 예를 들어 임립미술관·대통길작은미술관·
자연미술관Ko를 각각 휴식·체험·자연으로 나누고 싶어도, 셋의 원문이 모두 `미술관`이면 불가능하다.

- 장소마다 다르게 하고 싶으면 **`category_raw` 를 더 구체적으로 수집**한다.
  (실제 네이버 분류도 `미술관` / `갤러리` / `전시관` / `문화예술회관` 처럼 나뉘어 있다)
- 이 제약 자체가 좋은 수업 소재다 — "분류 체계는 데이터가 가진 만큼만 정밀해진다."

### 4.4 원본 CSV는 일부러 — 그리고 실제로 — 지저분하다 ★

네이버플레이스 데이터는 오염을 "주입"할 필요가 거의 없다. **실데이터가 원래 지저분하다.**
정제 기준 30행에 아래를 반영해 원본 **33행**을 만든다.

| 오염 유형 | 건수 | 내용 | 출처 |
|---|---|---|---|
| 중복 행 | 2 | 같은 장소가 지점명 표기 차이로 2번 수집 (`개성집` / `개성집공주점`) | 실제 발생 유형 |
| 리뷰수 콤마 표기 | 6 | `"1,204"` → 문자열이라 정렬·계산 불가 | 화면 복사 시 실제 발생 |
| 별점 결측 | 5 | 별점 없는 플레이스 → **보정 알고리즘이 처리** | 실데이터 그대로 |
| 좌표 결측 | 1 | mapx/mapy 누락 → 지도에 못 찍음, 제외 판단 실습 | 주입 |
| 카테고리 미매핑 | **33행 전부** | 원문 그대로 → 매핑표로 통일 | 실데이터 그대로 |

**중복 판정 규칙 (v1.0 미정 → 확정)**
공백 제거 + 지점 접미사(`공주점`·`본점`·`점`) 제거 후 이름이 같으면 중복.
남기는 행은 **`visitor_rev`가 큰 쪽** — 학생마다 다른 행을 남기면 `verify.py` 통과 여부가 갈린다.

**정제 정답: 33행 → 중복 2건 제거 · 좌표 결측 1건 제외 → 30행, 콤마 6건 숫자화, 카테고리 33건 매핑.**
이 숫자가 루브릭 '데이터 정제 20점'의 채점 기준이다.

### 4.5 파라미터 단일 출처 — `data/params.json` ★신규

v1.0의 가장 조용한 지뢰는 여기 있었다.
§5.1은 `C = 4.30`을 상수로 쓰면서 동시에 "`init_db.py`가 계산해 저장"이라고 적었다.
**수집 데이터가 조금만 달라져도 C가 4.28이 되고, 그러면 교재에 인쇄된 손계산 정답 4.34와
코드 출력이 어긋난다.** 검증 항목 4번은 그 순간 영구히 실패한다.

```json
{
  "m_review": 30,
  "c_mean": 4.30,
  "hand_calc": {
    "cafe":   {"name": "(A2에서 확정)", "v": 2,   "R": 5.0, "expect": 4.34},
    "bakery": {"name": "(A2에서 확정)", "v": 312, "R": 4.4, "expect": 4.39}
  },
  "collected_at": "2026-08"
}
```

- `scoring.py`·`verify.py`·교재 부록이 **모두 이 파일 하나를 읽는다.**
- `init_db.py`는 실측 평균을 **계산해서 출력만** 하고, `|실측 − c_mean| > 0.05`면 **경고를 띄우고 멈춘다.**
  → 교사는 `params.json`을 고치고 교재 예제를 다시 인쇄할지 판단한다. 조용히 넘어가지 않는다.
- `m = 30`은 "방문자리뷰 수 중앙값에서 채택"이라는 근거를 유지하되 **고정 상수**로 못박는다.
- `hand_calc`의 두 장소는 **A2 수집 완료 후 실측값으로 확정**한다. 그 전에는 교재를 인쇄하지 않는다(§8 임계 경로).

### 4.6 시드가 갖춰야 할 분포 조건 ★구현하며 확인 (§17-3)

임시 시드로 알고리즘을 돌려 보고 나서야 분명해진 것이 있다.
**"순위 뒤집힘 3곳"(§10-5)은 저절로 생기지 않는다.**

보정 전 랭킹도 동점을 방문자리뷰 수로 깬다(§5.5). 그래서 별점 등급이 같은 무리 안에서는
보정 전 순서와 보정 후 순서가 **거의 그대로다.** 순위가 크게 뒤집히는 곳은 딱 두 종류뿐이다.

| 유형 | 무슨 일이 일어나나 | 예 (임시 시드 실측) |
|---|---|---|
| **리뷰 아주 적고 별점 아주 높음** | 평균 쪽으로 크게 끌려 내려간다 | 리뷰 2개·5.0점 → **1위에서 12위로** |
| **리뷰 아주 많고 별점 높은 편** | 자기 별점을 거의 지켜 올라간다 | 리뷰 1,532개·4.6점 → **7위에서 2위로** |

첫 설계에서는 이런 장소가 2곳뿐이라 뒤집힘이 **2곳**밖에 안 나와 §10-5가 실패했다.
저리뷰·고별점 장소를 5곳으로 늘리자 **5곳**이 되었다.

**A2 수집 시 지킬 것**

- **리뷰 10개 미만이면서 별점 4.7 이상인 곳을 최소 4곳** 넣는다.
  실제로 흔하다 — 작은 카페·갤러리·체험마을이 대개 여기 해당한다.
- **리뷰 1,000개 이상인 곳도 최소 4곳** 넣는다. 올라갈 자리가 있어야 뒤집힘이 보인다.
- 별점이 4.0~4.5에만 몰린 목록은 아무리 30곳을 모아도 데모가 밋밋해진다.

---

## 5. 점수 알고리즘 (`scoring.py`) — 이 수업의 심장

### 5.0 함수 시그니처 (확정)

```python
CATEGORIES = ["역사문화", "자연", "체험", "먹거리", "카페감성", "휴식"]   # 순서 고정

def adjusted_star(star, visitor_rev, m, C) -> float           # star=None이면 R=C
def star_score(adj, adj_min, adj_max) -> float                # 0~1 정규화 ★신규
def haversine_km(lat1, lon1, lat2, lon2) -> float             # geo.py에서 이식
def distance_score(km) -> float                               # 1 / (1 + km)
def interest_map(user_id, conn) -> (dict, int)                # (카테고리별 관심도, 로그건수)
def pref_boost(category, interests, log_count) -> float       # 0~1, 균등이면 0.5
def final_score(s_star, s_dist, s_pref, w) -> float           # w = (0.5, 0.3, 0.2)
def rank_places(places, origin_id, user_id, conn, w) -> list  # 정렬 + 동률 규칙
```

`interest_map`이 **관심도와 로그 건수를 함께 반환**하는 것이 핵심 설계다.
콜드 스타트 여부를 화면에 그대로 노출("아직 클릭 기록이 적어 취향은 반영되지 않았습니다")하기 위함이다.
v2.4에서 `collab_score`가 근거 문구를 함께 반환한 것과 같은 원칙 — **폴백을 숨기지 않는다.**

### 5.1 보정 별점 — 베이지안 평균 ★손계산 차시

문제 제기부터 시작한다: **"리뷰 2개짜리 5.0점 카페와 리뷰 312개짜리 4.4점 베이커리, 어디가 진짜 맛집인가?"**

```
보정별점 = (v × R + m × C) / (v + m)

v = 그 장소의 방문자리뷰 수
R = 그 장소의 별점            (결측이면 R = C → 자연히 평균에 수렴)
m = 기준 리뷰 수 = 30         (params.json, 고정)
C = 전체 평균 별점 = 4.30     (params.json, 고정 / init_db가 실측과 대조)
```

**손계산 검증 예제 (학생이 직접 푼다)**

```
카페 A    : v=2,   R=5.0 → (2×5.0 + 30×4.30) / 32  = 139.0  / 32  = 4.34
베이커리 B : v=312, R=4.4 → (312×4.4 + 30×4.30) / 342 = 1501.8 / 342 = 4.39

→ B 승리. "리뷰가 적은 별점은 전체 평균 쪽으로 끌려온다"
```

**결측 별점의 성질을 수업에서 반드시 짚는다.**
R = C를 넣으면 식이 `(v×C + m×C) / (v+m) = C`로 **정확히 4.30에 고정된다.**
리뷰가 2개든 500개든 결과가 같다 — 별점이 없으면 리뷰 수는 아무 정보도 주지 못하기 때문이다.
그 결과 **결측 5곳은 보정별점이 완전히 동률**이 되므로, 정렬에는 §5.5의 동률 규칙이 필요하다.
v1.0은 이 동률을 예상하지 못했다.

### 5.2 별점 정규화 — v1.0의 가장 큰 결함을 고치는 곳 ★

v1.0은 `0.5 × (보정별점 / 5)`를 썼다. 수집 데이터로 계산해 보면 보정별점은
대략 **4.1~4.7** 구간에 몰린다(A2 완료 후 실측으로 확인할 것). /5 하면 0.82~0.94, 즉 **변별폭 0.12**.
가중치 0.5를 곱하면 최종 점수에 **약 0.06**만큼 기여한다.

같은 계산을 거리 항에 하면 — 공주 30곳은 반경 30km에 퍼져 있어 거리 점수가 **0.03~1.0**,
가중치 0.3을 곱해 **0.29**. 별점의 약 5배다.

> **가중치는 0.5 : 0.3인데 실제 영향력은 1 : 5.**
> 학생이 두 차시를 들여 배운 보정 별점이 화면에서는 거의 보이지 않는다.

**확정: 30곳 전체 기준 min-max 정규화**

```
별점점수 = (보정별점 − adj_min) / (adj_max − adj_min)      # 0~1, meta 테이블 값 사용
```

- 최하위 장소가 0, 최상위가 1이 되어 **가중치 0.5가 액면 그대로 작동**한다.
- `adj_min`/`adj_max`는 `init_db.py`가 계산해 `meta`에 저장 → 요청마다 흔들리지 않는다.
- 수업 포인트: **"정규화는 절대 평가를 상대 평가로 바꾼다."**
  4.10점이 0점이 되는 게 부당해 보이는가? 그 감각이 정확히 정규화의 대가다.
  `/stats`에 보정별점 원값과 정규화값을 **나란히** 보여 주고 이 질문을 던진다.

### 5.3 거리 점수 — 기준점을 확정한다

```
거리 점수 = 1 / (1 + km)        # 하버사인. 0km→1.0, 1km→0.5, 4km→0.2, 14.7km→0.06
```

**기준점 (v1.0 미정 → 확정)**
v1.0은 "현재 보고 있는 장소 또는 지도 중심"이라 했지만, 지도 중심을 서버가 알려면
JS가 좌표를 AJAX로 보내야 한다 — §6.3의 JS 경계를 넘는다.

- 기준점은 지도 상단 **`<select>` 폼**에서 고른다 → `GET /?origin=<id>`
- 기본값은 **공산성**(원도심 앵커, id 1)
- `/panel/<id>`를 열면 그 장소를 `session["origin_id"]`에 저장 → 지도로 돌아오면 그곳이 기준점
- **JS 0줄.** "방금 본 곳에서 가까운 순서로 다시 줄 세우기"라는 탐색 루프가 폼 제출만으로 성립한다

하버사인은 v2.4 `geo.py`를 그대로 이식한다(자산 재활용). 검산값도 함께 가져온다:
공산성→국립공주박물관 1.3km, 공산성→마곡사 14.7km, 마곡사→동학사 29.4km.

### 5.4 취향 부스트 — 클릭 로그로 배우는 암묵 피드백

폼으로 묻지 않는다. **행동이 곧 취향이다.**

```
카테고리 관심도 = (그 카테고리 view 수 × 1 + 현재 찜 수 × 3) / (전체 view × 1 + 전체 찜 × 3)
취향 부스트    = 관심도 / (관심도 + 1/6)        # ★ v1.0에서 변경
로그 5건 미만  → 부스트 0.5 고정                # 콜드 스타트
```

**왜 나눗셈을 한 번 더 하나 (v1.0의 문제 2)**
6분류에서 관심도가 균등하면 값은 1/6 = 0.167이다. v1.0은 이걸 그대로 부스트로 썼다.
그러면 아무 행동도 안 한 사용자의 부스트가 0.167 — 다른 두 항이 0.5 안팎에서 노는 동안
**부스트만 항상 바닥**에 있다. 가중치 0.2가 사실상 0.03으로 줄어든다.

`관심도 / (관심도 + 1/6)`로 바꾸면 균등에서 **정확히 0.5(중립)**, 한 카테고리에 몰리면 0.86,
전혀 안 본 카테고리는 0에 가까워진다. 스케일이 다른 두 항과 맞는다.

> **덤으로 이 식은 §5.1 베이지안 보정과 같은 꼴이다** — `x / (x + 기준값)`.
> "관심도를 균등이라는 기준으로 끌어당긴다"고 설명하면 학생은 같은 아이디어를 두 번 만난다.
> 수업에서 이 대칭을 반드시 짚는다.

**찜 수는 `clicks`가 아니라 `favorites` 테이블 기준으로 센다 (v1.0 미정 → 확정).**
클릭 로그로 세면 찜했다 취소한 기록이 그대로 남아 취향이 왜곡된다. 현재 찜 상태로 세면 상쇄가 자동이다.
`unfav` 이벤트는 로그로만 남기고 점수에는 쓰지 않는다.

**view 로그 중복 방지 (v1.0 미정 → 확정).**
같은 사용자·같은 장소의 `view`는 **10분 내 재기록하지 않는다.**
새로고침 몇 번으로 한 카테고리 관심도가 0.8까지 치솟는 사고를 막는다.

### 5.5 최종 점수와 정렬

```
final = 0.5 × 별점점수 + 0.3 × 거리점수 + 0.2 × 취향부스트      # 세 항 모두 0~1
```

- 가중치 3개는 지도 상단 폼에서 조절(`?w_star=&w_dist=&w_pref=`). **합이 1이 아니면 합으로 나눠 자동 정규화**한다 — 학생이 0.9/0.9/0.9를 넣어도 서비스가 깨지지 않는다.
- **동률 규칙 (v1.0 없음 → 확정)**: `final` 내림차순 → `visitor_rev` 내림차순 → `id` 오름차순.
  §5.1에서 본 대로 별점 결측 5곳은 보정별점이 정확히 같으므로 이 규칙이 없으면
  **새로고침할 때마다 순위가 바뀐다.**
- 정렬이 바뀌면 **지도 마커 크기가 점수 순으로 3단계** 변하고, 우측 랭킹 목록이 다시 그려진다.

### 5.6 동선 정렬 (`route.py`) — 찜 목록을 걷는 순서로

코스를 "추천"하지 않는다. **학생이 찜한 곳을 정렬만 해 준다.** (v2.4와 역할 분담이 다른 지점)

```
① 찜 목록에서 가장 북쪽 장소를 출발점으로
② 남은 곳 중 가장 가까운 곳으로 이동 (최근접 이웃 탐욕법)
③ 반복 → 방문 순서 + 구간별 이동시간 표시
④ 화면에 "이 방법은 최적해를 보장하지 않는다"를 명시 — 탐욕 알고리즘의 한계도 교육 내용
```

**이동시간 (v1.0의 문제 5 → 확정)**

```python
def travel_minutes(km):
    if km <= 3.0:  return round(km * 1.3 / 4.0  * 60), "도보"
    else:          return round(km * 1.3 / 40.0 * 60), "차량"
```

v1.0은 도보 4km/h 고정이었다. 공주는 그렇게 생기지 않았다 —
공산성→마곡사 14.7km면 화면에 **"도보 286분"**이 찍힌다. 3km 경계로 차량 전환하면 **약 29분**,
v2.4가 실측 검산한 "공산성→마곡사 30분"과 맞아떨어진다.
배지로 `도보 12분` / `차량 29분`을 구분 표시하고, **차량 구간이 하나라도 있으면
"이 동선은 걸어서 다닐 수 없습니다"를 상단에 띄운다.**

**찜이 1곳 이하면** 동선 화면 대신 "두 곳 이상 찜해 주세요" 안내로 분기한다(v1.0 미처리).

---

## 6. 화면 설계

### 6.1 라우팅 7개 (쿼리 파라미터 확정)

| 메서드 | 경로 | 화면/처리 | 핵심 요소 |
|---|---|---|---|
| GET | `/` | **지도 홈** | `?cat=&origin=&w_star=&w_dist=&w_pref=` · Leaflet 지도 + 마커 30개(점수순 3단계 크기) + 카테고리 칩 6개 + 기준점 select + 가중치 폼 + 우측 랭킹 |
| GET | `/panel/<id>` | 상세 (독립 페이지) | 별점·**보정별점 비교 표시**·방문자/블로그 리뷰수·주소·출처 표기, [찜] 버튼. 진입 시 `view` 로그 + `origin_id` 세션 저장 |
| POST | `/fav/<id>` | 찜 토글 | favorites INSERT/DELETE + clicks 로그 → 이전 화면으로 리다이렉트 |
| GET | `/favorites` | 찜 목록 | 찜 카드 + [동선 짜기] |
| POST | `/route` | 동선 결과 | 순번 지도 + 구간별 이동시간 + 총합 + 탐욕법 한계 문구 |
| GET | `/stats` | 대시보드 | **보정 전/후 랭킹 비교표**, 카테고리별 리뷰수 분포(CSS 막대), 내 클릭 로그 요약 |
| GET | `/offline` | SVG 산점도 | 타일 없이도 30곳의 위치 관계를 보는 화면 |

### 6.2 사용자 식별 — Flask session (v1.0 누락 → 확정)

v1.0에는 `users` 테이블이 있는데 **사용자를 만드는 시점이 어디에도 없었다.**
홈이 입력 폼이 아니라 지도이므로 v2.4처럼 "제출 시 INSERT"를 쓸 수 없다.

- `@app.before_request`에서 `session["user_id"]`가 없으면 **자동 INSERT** (`nickname='guest-1234'`)
- 상단에 닉네임 변경 폼(선택 사항) — 발표 때 누구 화면인지 구분하는 용도
- `app.secret_key`는 하드코딩하되 *"실제 서비스라면 비밀로 관리한다"* 주석을 반드시 단다

### 6.3 JS 경계 — 못박는다

학생이 작성(프롬프트로 생성)하는 JS는 **`map.html` 안의 1블록**이 전부다:
지도 초기화 → 서버가 Jinja2로 심어 준 JSON으로 마커 찍기 → 마커 클릭 시 `/panel/<id>`로 **이동만**.

팝업·애니메이션·AJAX·좌표 전송은 쓰지 않는다. 패널·찜·동선·필터·가중치·기준점은 **전부 서버 렌더 + 폼 제출**.

> **"JS는 지도에만, 로직은 파이썬에만."** 이 경계가 디버깅 지옥을 막는다.
> v1.0의 §5.2(지도 중심 기준점)와 §6.3(자동 폴백)은 둘 다 이 경계를 몰래 넘고 있었다.

### 6.4 오프라인 판정 — "자동 폴백"의 정체를 확정한다

v1.0은 "타일 로딩 실패 시 자동 전환"이라 썼지만, 타일 실패는 브라우저에서 일어나는 사건이라
감지하려면 JS 이벤트 핸들러가 필요하다. 서버는 그 사실을 알 수 없다.

**확정: 서버가 기동할 때 한 번 확인한다.**

```python
# app.py 시작 시 1회. 매 요청마다 하지 않는다(느려진다).
OFFLINE = not _ping("https://tile.openstreetmap.org/0/0/0.png", timeout=2.0)
```

- `OFFLINE`이면 `map.html`이 Leaflet 대신 **인라인 SVG 산점도**를 렌더하고 상단에 "오프라인 모드" 배너를 띄운다.
- 인라인 SVG는 `geo.to_svg_points()`(v2.4 확장 3)를 재사용하며 **현재 점수·필터가 그대로 반영**된다. 정적 파일이었다면 불가능했다.
- v2.4가 겪은 두 문제도 함께 상속한다: 축척은 **표시 대상 기준 1.6배 여유**(최소 위도 0.02도), **이름표는 빼고 번호만** 찍고 이름은 옆 목록이 담당한다.
- 학교망이 불안정해 판정이 틀릴 때를 대비해 상단에 **수동 전환 링크**(`/offline`)를 항상 노출한다.
- `verify.py` 11번이 **인터넷을 끊은 상태에서 전 라우트 200**을 확인한다.

### 6.5 디자인 규칙 (v1.0 내부 모순 해소)

v1.0은 "색상 3개 이내"라고 해 놓고 바로 다음 줄에 "카테고리별 색"(6색)을 요구했다. 확정:

- **UI 기본 색 3개** (배경·본문·강조) — 버튼·카드·헤더는 여기서만 쓴다
- **카테고리 팔레트 6색은 지도 마커와 범례에만 허용되는 예외** — 색이 곧 데이터인 유일한 자리
- 지도 70% : 랭킹 패널 30% (모바일은 지도 위 / 목록 아래 1컬럼)
- 마커 크기 3단계(final 상·중·하), CSS는 `style.css` 한 파일

---

## 7. 개발 단계 (Step 0~8) · 50분 × 12차시

| 차시 | Step | 내용 | 산출물 |
|---|---|---|---|
| 1 | 0 | 환경 설치, Hello 서버, Leaflet 번들 확인 | 서버 실행 성공 |
| 2 | 1 | `places_raw.csv` 점검표 — 콤마·결측·중복 찾기 | 데이터 점검표 |
| 3 | 2 | 카테고리 매핑표 작성 + 정제 + DB 적재 | 33→30행 검증 로그 |
| 4 | 3 | 지도에 마커 30개 찍기 | 첫 "우와" 지점 |
| **5-6** | **4** | **보정 별점 + 손계산 검증 (핵심 차시 — 절대 줄이지 않는다)** | 보정별점 동작 + 손계산지 |
| 7 | 5 | 정규화 + 거리 점수 + 취향 부스트 + final 결합 | 랭킹 동작 |
| 8 | 6 | 상세 패널 + 찜 + 클릭 로그 | 탐색 루프 완성 |
| 9 | 7 | 동선 정렬 + `/stats` 보정 전/후 비교 | 서비스 완성 |
| 10-11 | 8 | 가중치 튜닝, 오프라인 폴백 확인, 오류 수정 루프 | 시연 준비 |
| 12 | 8 | 발표 — **"보정으로 순위가 뒤집힌 장소 1곳"**을 반드시 설명 | 발표자료 |

> **3단계(생성형 AI 활용)는 별도 차시가 아니라 Step 1~8 전체에 녹아 있다.**
> 모든 단계에서 학생은 프롬프트 → 실행 → 오류 → 재질문 루프를 돈다.

5-6차시가 이 수업의 심장이다. §12 루브릭의 '보정 별점 이해' 25점이 여기서 살거나 죽는다.

---

## 8. 사전 제작 작업 순서 (교사 트랙)

| 트랙 | 주체 | 산출물 | 시점 |
|---|---|---|---|
| **A. 사전 제작** | 교사 | 레퍼런스 완성본, 수집 CSV, Step별 스냅샷, 배포 키트 | 수업 전 |
| **B. 수업 재현** | 학생 | Claude Code 프롬프트로 A를 스스로 다시 만듦 | 수업 중 |

**A를 100% 끝내야 B가 안전하다.** 학생이 막혔을 때 붙여넣을 정답이 없으면 수업이 멈춘다.

| # | 작업 | 완료 기준 | 예상 |
|---|---|---|---|
| A1 | 폴더 뼈대 + Leaflet 로컬 번들 + Hello 서버 | 인터넷 끊고도 지도 라이브러리 로드됨 | 1h |
| **A2** | **네이버플레이스 30곳 수집** (§4.1·4.3 분포 조정 포함) | 6분류 각 4곳 이상, 좌표는 v2.4에서 이식 | **3h** |
| A3 | `params.json` 확정 + 손계산 예제 2곳 선정 | 실측 평균과 `c_mean` 차이 ≤ 0.05 | 0.5h |
| A4 | `places_raw.csv` 오염 33행 생성 | §4.4 표대로 5종 | 1h |
| A5 | `preprocess.py` (콤마·중복·결측·매핑) | 33→30, 미매핑 원문 목록 출력 | 1.5h |
| A6 | `init_db.py` (5테이블, `adj_star`·`meta` 계산, `--reset` 분기) | 재실행 안전 | 1.5h |
| A7 | `db.py` 조회 함수 | 전 라우트가 쓸 함수 완비 | 1h |
| A8 | `scoring.py` — 보정별점·정규화 | 손계산 4.34 / 4.39와 일치 | 2h |
| A9 | `scoring.py` — 거리·부스트·final·동률 | 콜드스타트 0.5 확인 | 1.5h |
| A10 | `route.py` (NN + 도보/차량 분기) | 4곳 전수 24순열 평균보다 짧음 | 1.5h |
| A11 | `geo.py` 이식 + 인라인 SVG 폴백 | 인터넷 끊고 전 라우트 200 | 1.5h |
| A12 | 템플릿 7종 + `style.css` + Leaflet 1블록 | 7화면 전부 렌더 | 4h |
| A13 | `verify.py` (§10 13항목) | 전항목 PASS | 1.5h |
| A14 | Step별 스냅샷 + 프롬프트 대본 완주 1회 + 배포 키트 | 각 스냅샷 단독 실행, 오프라인 설치 성공 | 3h |
| | **합계** | | **약 25.5h (3~4일)** |

**임계 경로: A2 → A3 → A4 → A6 → A8 → A13**

- **A2가 부실하면 전부가 무너진다.** 별점·리뷰수 분포가 밋밋하면 §10-5(순위 뒤집힘 3곳)가 실패하고,
  그러면 12차시 발표에서 학생이 설명할 것이 없어진다.
- **A3 전에는 교재를 인쇄하지 않는다.** 손계산 예제 수치가 확정되기 전에 인쇄하면 §4.5의 사고가 그대로 재발한다.

---

## 9. Claude Code 프롬프트 대본 (발췌)

학생은 아래를 **순서대로 복사·입력**한다. 전문은 교재 부록으로 제본하고, `check_prompts.py`로 코드와의 정합성을 검사한다.

**Step 2 — 정제**

```
data/places_raw.csv를 읽어 정제하는 scripts/preprocess.py를 만들어줘.
(1) 리뷰수의 콤마("1,204")를 제거해 정수로 변환
(2) 장소명 중복 제거 — 공백과 '공주점'·'본점' 같은 지점 접미사를 뗀 뒤 비교하고,
    중복이면 방문자리뷰 수가 큰 행을 남겨줘
(3) 좌표(lat/lon) 결측 행은 제외하고 몇 행을 제외했는지 print
(4) category_map.csv를 읽어 category_raw를 표준 6분류로 매핑,
    매핑에 없는 원문이 나오면 오류 대신 목록으로 출력해줘 (내가 매핑표에 추가할 수 있게)
encoding='utf-8-sig' 고정. 줄마다 한글 주석.
```

**Step 4 — 보정 별점**

```
scoring.py에 adjusted_star(star, visitor_rev, m, C) 함수를 만들어줘.
(v×R + m×C) / (v+m) 공식이고, star가 None이면 R=C로 계산해줘.
m과 C는 data/params.json에서 읽어와줘. 중간 계산(분자·분모)을 print로 보여줘.
넘파이 없이 순수 파이썬으로.
```

**Step 5 — 정규화와 최종 점수**

```
scoring.py에 star_score(adj, adj_min, adj_max)를 추가해줘.
(adj - adj_min) / (adj_max - adj_min) 으로 0~1로 펴 주는 함수야.
adj_min/adj_max는 meta 테이블에서 읽어와줘.
그리고 final_score(별점점수, 거리점수, 취향부스트, w)를 만들어줘.
w는 (0.5, 0.3, 0.2)이 기본이고, 합이 1이 아니면 합으로 나눠서 정규화해줘.
```

**Step 5 — 지도 마커**

```
map.html에서 Leaflet 지도를 띄우고 서버가 넘겨준 places JSON으로 마커를 찍어줘.
마커를 클릭하면 /panel/<id> 로 이동만 하면 돼. 팝업이나 애니메이션은 넣지 마.
leaflet은 static/leaflet/ 로컬 파일을 쓰고, 서버가 넘겨준 offline 값이 True면
지도 대신 오프라인 안내 배너와 /offline 링크를 보여줘.
```

---

## 10. 검증 체크리스트 (`verify.py`) — 13항목 + 확장 회귀 3항목

**수업 전날 이 스크립트 하나로 전부 통과해야 한다.**

결과는 **세 가지**다 (v2.1에서 추가).

| 표시 | 뜻 |
|---|---|
| `[ O ] PASS` | 통과 |
| `[ X ] FAIL` | 고쳐야 한다 |
| `[ ? ] PENDING` | **코드는 맞는데 A2 수집이 아직이라 판단할 수 없다** |

세 번째가 필요한 이유: 임시 시드로 돌리는 동안 2번(분류별 4곳 이상)은 구조적으로 통과할 수
없다. 이걸 `FAIL`로 찍으면 "코드가 틀렸다"는 신호와 구별되지 않아서, 진짜 실패를 놓치게 된다.

| # | 항목 |
|---|---|
| 0 | `places_raw.csv` 33행 → 정제 후 30행 (중복 2 제거, 좌표결측 1 제외) |
| 1 | 리뷰수 컬럼 전부 정수형, 콤마 잔존 0건 |
| 2 | category 전부 표준 6분류 안, 미매핑 0건, **6분류 각각 4곳 이상** |
| 3 | 별점 결측 5건이 보정별점에서는 결측 0건이고 **전부 정확히 C(4.30)** |
| 4 | 손계산 예제 — `params.json`의 `expect` 값과 코드 출력이 **소수 2자리 일치** |
| 5 | **보정 전/후 랭킹에서 순위 변동 3계단 이상인 장소가 3곳 이상** ← 데모 성패 |
| 6 | 정규화 후 별점점수의 min = 0.0 / max = 1.0 |
| 7 | 거리점수: 같은 좌표 1.0, 1km 0.5 (소수 3자리) |
| 8 | 로그 5건 미만 사용자 → 취향 부스트 **0.5**, 균등 분포 사용자도 0.5 |
| 9 | 찜 4곳 동선: NN 결과가 **24개 전체 순열 평균보다 짧음** (전수 계산, 무작위 아님) |
| 10 | 3km 초과 구간이 **'차량'으로 표시**되고 공산성→마곡사가 30 ± 3분 |
| 11 | 인터넷 차단 상태에서 **전 라우트 200 응답** (오프라인 폴백) |
| 12 | `init_db.py` 인자 없이 재실행해도 찜·로그 보존 / `--reset`일 때만 재적재 |

**5번이 실패하면 시드의 별점·리뷰수 분포가 밋밋한 것이다 — A2(수집)로 돌아간다.**
"순위 뒤집힘"의 정의도 확정한다: **별점이 있는 25곳만** 대상으로, 보정 전 랭킹(`star` 내림차순,
동률은 `visitor_rev` 내림차순)과 보정 후 랭킹의 순위 차가 3계단 이상인 장소를 센다.
결측 5곳은 보정 전 랭킹이 존재하지 않으므로 비교 대상에서 제외한다(v1.0 미정의).

---

## 11. 실행 방법 (학생용 3줄)

```bash
pip install flask pandas          # 최초 1회
python scripts/init_db.py         # DB 만들기 (최초 1회)
python app.py                     # 서버 켜기 → 127.0.0.1:5000
```

처음부터 다시 만들려면 (경고: 찜과 클릭 로그가 사라짐)

```bash
python scripts/init_db.py --reset
```

포트가 충돌하면 `app.run(port=5050)`. 옆 조에 보여주려면 `host="0.0.0.0"`으로 바꾸고
같은 와이파이에서 `http://내IP:5000`으로 접속한다.

---

## 12. 평가 루브릭 (100점)

| 항목 | 배점 | 확인 방법 |
|---|---|---|
| 데이터 정제 | 20 | 33→30행, 콤마·중복·매핑 처리 설명 |
| 보정 별점 이해 | 25 | **손계산 1건 + "순위가 뒤집힌 이유" 설명** |
| 지도 서비스 동작 | 25 | 지도→패널→찜→동선 전 과정 시연 |
| AI 협업 과정 | 15 | 프롬프트·오류 수정 기록 3건 |
| 발표·기획 | 15 | 개선 아이디어 1가지 (예: 리뷰 텍스트 감성 분석) |

---

## 13. 확장 과제 (기본값 꺼짐 원칙 — v2.4 계승)

확장 기능은 전부 *기본값이 꺼짐*인 선택 인자로 붙인다. 아무것도 주지 않으면 기본 과제와 똑같이 동작하며,
`verify.py` 13항목이 회귀 테스트로 이를 보증한다.

| # | 확장 | 구현 | 켜는 법 | 상태 |
|---|---|---|---|---|
| 1 | 영업시간 반영 ("지금 열었나") | 수집 항목에 영업시간 추가, 현재 시각 비교 배지 | `?open_now=1` | ⬜ **A2 대기** — 영업시간을 수집해야 만들 수 있다 |
| 2 | 블로그리뷰수를 점수에 반영 | `adjusted_star(..., use_blog=True)`. 보정별점의 v를 `visitor + 0.3 × blog`로. 값은 `places.adj_star_blog` 컬럼에 미리 계산해 둔다 | 지도 상단 '심화 설정' 칩 (`?use_blog=1`) | ✅ |
| 3 | 두 사람 찜 합치기 | `db.merged_favorite_places()` — 두 세션의 favorites 합집합으로 동선 | 찜 목록에서 짝꿍의 **공유 코드** 입력 | ✅ |
| 4 | 히트맵 | `db.click_counts()` — 마커 크기를 점수 대신 누적 클릭 수로 | 지도 상단 '심화 설정' 칩 (`?heat=1`) | ✅ |
| 5 | 실시간 API 전환 (심화반) | 네이버 지역검색 API로 CSV를 갱신하는 `refresh.py` | 별도 실행 | ⬜ **API 키 발급 필요** |

**"기본값 꺼짐"은 말이 아니라 테스트로 지킨다.**
`verify.py` 13번이 *확장을 켜지 않은 화면의 랭킹 순서가 기본과 완전히 같은지* 매번 확인한다.
14·15번은 확장 2·3이 실제로 동작하고, 잘못된 입력(없는 짝꿍 코드)에도 안전한지 본다.

---

## 14. 리스크와 대비

| 리스크 | 심각도 | 대비 |
|---|---|---|
| **A2 수집 데이터가 밋밋함** | **높음** | 검증 5번이 사전 차단. 실패 시 리뷰수 편차가 큰 장소로 목록 교체 |
| 학교망에서 OSM 타일 차단 | 높음 | 서버 기동 시 판정 + 인라인 SVG (§6.4), 검증 11번 |
| 한글 CSV 인코딩 깨짐 | 높음 | `utf-8-sig` 고정, "CSV를 Excel로 열어 저장하지 말 것" 명시 |
| **윈도우 콘솔 한글 출력 오류** ★신규 | **높음** | 모든 스크립트가 `import console_utf8` (§17-1). CSV 인코딩과 **다른 문제**이고 1차시에 바로 만난다 |
| 학생별 진도 편차 | 높음 | `snapshots/step3_done`~`step7_done` 사전 준비, 뒤처진 학생은 복사해서 이어감 |
| Leaflet JS 오류에 학생이 막힘 | 중간 | JS는 `map.html` 1블록으로 격리, 완성 블록을 스냅샷으로 제공 |
| 리뷰수·별점이 수업 시점에 변동 | 중간 | CSV에 수집일 명기, "스냅샷 데이터"임을 명시 — 오히려 데이터 시의성 교육 소재 |
| 별점 결측이 계산을 깨뜨림 | 중간 | R=C로 알고리즘이 흡수 + 동률 정렬 규칙(§5.5) |
| pip / pandas 설치 실패 | 중간 | 오프라인 whl USB 배포, `preprocess.py`를 표준 `csv`로도 동작하게 이중 작성 |
| DB 초기화 사고 | 중간 | `init_db.py`는 `--reset` 없이는 덮어쓰지 않음 |
| 네이버 데이터 이용 범위 | 중간 | 화면에 출처("네이버플레이스, 2026-08 수집") 표기, **교육 목적 내부 실습 한정·재배포 금지**, 자동 크롤링이 아닌 수기 수집 |
| OSM 타일 이용약관 | 낮음 | 학급 규모(30명) 사용은 허용 범위. 대량 자동 요청 금지를 코드 주석에 명시 |

가장 위험한 것은 **A2 수집**이다. 코드는 틀리면 고칠 수 있지만, 데이터가 밋밋하면 12차시 발표에서
학생이 설명할 "뒤집힌 장소"가 존재하지 않는다. 이건 수업 당일에는 고칠 수 없다.

---

## 15. v1.0 → v2.0 변경 이력

| # | v1.0 상태 | v2.0 확정 | 근거 |
|---|---|---|---|
| 1 | `final`에 `보정별점 / 5` | **min-max 정규화** (§5.2) | 실데이터 별점 폭이 좁아 별점 항의 실효 영향이 거리 항의 1/5로 눌림 |
| 2 | 취향 부스트 = 관심도(균등 0.167) | **관심도 / (관심도 + 1/6)**, 균등 0.5 | 다른 두 항과 스케일 불일치로 가중치 0.2가 사실상 0.03 |
| 3 | 거리 기준점 = "지도 중심" | **select 폼 + 세션 저장**, 기본 공산성 | 지도 중심 취득은 AJAX 필요 → §6.3 JS 경계 위반 |
| 4 | `static/fallback_map.svg` 자동 전환 | **서버 기동 시 타일 확인 + 인라인 SVG** | 타일 실패 감지는 JS 영역 / 정적 SVG는 점수·필터 미반영 |
| 5 | 도보 4km/h 고정 | **3km 초과 시 차량 40km/h + 배지** | 공산성→마곡사 14.7km가 "도보 286분"으로 표시됨 |
| 6 | C를 "상수 4.30"이자 "init_db가 계산" | **`params.json` 단일 출처 + 실측 대조 경고** | 수집값이 바뀌면 교재의 손계산 정답과 코드가 영구 불일치 |
| 7 | 정렬 동률 규칙 없음 | **final → visitor_rev → id** | 결측 5곳의 보정별점이 정확히 C로 동률 → 순위가 매번 바뀜 |
| 8 | `users` 생성 시점 없음 | `before_request` 자동 INSERT (§6.2) | 홈이 폼이 아니라 지도라 v2.4 방식을 쓸 수 없음 |
| 9 | `panel.html`이 "부분 템플릿" | **독립 페이지** | AJAX를 안 쓰기로 했으므로 부분 템플릿이 성립하지 않음 |
| 10 | 카테고리 색 6개 + "색상 3개 이내" | UI 3색 + **지도 마커에 한해 6색 예외** | 기획안 내부 모순 |
| 11 | 시드 30곳 그대로 재사용 | **카테고리 목표 분포 + 교체 규칙** (§4.3) | 그대로 쓰면 카페감성 2곳·휴식 3곳 → 필터 칩 2개가 사실상 빈 화면 |
| 12 | 중복 2건의 판정·잔존 규칙 없음 | 접미사 제거 후 비교, `visitor_rev` 큰 행 채택 | 학생마다 다른 행을 남기면 검증 결과가 갈림 |
| 13 | view 로그 무제한 기록 | **10분 내 중복 미기록** | 새로고침만으로 취향이 왜곡됨 |
| 14 | 찜 수를 clicks에서 집계 | **favorites 현재 상태 기준** | 찜 취소가 상쇄되지 않음 |
| 15 | 검증 5번 "뒤집힘"의 정의 없음 | 별점 보유 25곳 대상, 3계단 이상 | 판정 기준이 없으면 통과 여부를 말할 수 없음 |
| 16 | 검증 8번 "무작위 순서 평균" | **4곳 전수 24순열 평균** | 확률적 기준은 재현되지 않는 테스트 |
| 17 | 교사 트랙 작업표 없음 | **A1~A14, 25.5h, 임계 경로 명시** (§8) | v2.4에서 이 표가 없으면 수업 전날 사고가 난다는 것이 확인됨 |
| 18 | 가중치 합 검증 없음 / 찜 0~1곳 처리 없음 | 합으로 자동 정규화 / 안내 화면 분기 | 학생이 반드시 시도하는 입력 |

---

## 16. 구현 상태 (2026-08-30 기준)

**코드는 완성되어 동작한다. 남은 것은 A2(사람이 하는 데이터 수집) 하나다.**

### 16.1 트랙 A 진행 상황

| # | 작업 | 상태 |
|---|---|---|
| A1 | 폴더 뼈대 + Hello 서버 | ✅ |
| **A2** | **네이버플레이스 30곳 수집** | ⬜ **미착수 — 사람이 해야 하는 유일한 작업** |
| A3 | `params.json` 확정 | ◐ 임시값으로 채워 둠. A2 후 실측으로 교체 |
| A4 | `places_raw.csv` 오염 33행 | ✅ (`make_placeholder_seed.py`가 생성) |
| A5 | `preprocess.py` | ✅ 33 → 30행 |
| A6 | `init_db.py` (5테이블·`--reset`) | ✅ |
| A7 | `db.py` | ✅ |
| A8 | `scoring.py` 보정별점·정규화 | ✅ |
| A9 | `scoring.py` 거리·부스트·final·동점 | ✅ |
| A10 | `route.py` | ✅ |
| A11 | `geo.py` + 인라인 SVG 폴백 | ✅ |
| A12 | 템플릿 8종 + `style.css` | ✅ |
| A13 | `verify.py` 13항목 | ✅ |
| A14 | 스냅샷·대본 검증·배포 키트 | ◐ `make_dist.py`·`make_snapshots.py`·`check_prompts.py` 완료 / 오프라인 whl 미포함 |

### 16.2 검증 결과 — 통과 12 · 실패 0 · 수집대기 1

| 항목 | 실측값 |
|---|---|
| 정제 | 33행 → 30행 |
| 별점 결측 5곳의 보정 별점 | 전부 정확히 4.300 |
| 손계산 예제 | 4.34 / 4.39, 코드와 소수 2자리 일치 |
| 정규화 | min 0.000 / max 1.000 |
| 순위 3계단 이상 변동 | **5곳** (곡물집 1위→12위, 마곡사 7위→2위) |
| 동선 (찜 4곳) | 최근접이웃 3.54km · 24순열 평균 4.31km · 최적 3.23km (최적 대비 +9.8%) |
| 공산성 → 마곡사 | 14.7km → **차량 29분** (v2.4 실측 30분과 일치) |
| 오프라인 전 라우트 | 8개 전부 200 |
| `init_db` 재실행 | 찜·로그 보존 확인 |
| **분류별 4곳 이상** | **`[ ? ]` 카페감성 2곳 — A2 대기** |

### 16.3 데이터의 현재 상태 — 무엇이 진짜인가

| 실제 | 임시(placeholder) |
|---|---|
| 장소명 30곳 (v2.4에서 실재 확인) | 별점 |
| 좌표 lat/lon (네이버 mapx/mapy) | 방문자리뷰 수 · 블로그리뷰 수 |
| | 도로명주소 · 카테고리 원문 |

**임시 상태를 숨기지 않는다.** 화면 상단 배너 · `init_db.py` 출력 · `verify.py` 결과에
모두 표시된다. `params.json` 의 `data_source` 를 `"naver_place"` 로 바꾸는 순간
배너가 출처 표기로 바뀐다. 이 한 줄이 "진짜 데이터인가"의 유일한 스위치다.

임시값도 아무 숫자가 아니라 §4.6의 분포 조건을 만족하도록 설계했다.
그래서 A2 이전에도 파이프라인 전체를 끝까지 돌려 볼 수 있다.

### 16.4 다음 액션 — 순서대로

1. **A2 (3h)** — `scripts/collect_guide.md` 그대로 따라간다. §4.3 분포(카페감성 보강)와
   §4.6 조건(저리뷰·고별점 4곳, 리뷰 1,000개 이상 4곳)을 함께 만족시켜야 한다.
2. **A3 (0.5h)** — `init_db.py` 가 찍는 실측 평균으로 `c_mean` 확정, 손계산 예제 2곳 실측 교체,
   `data_source` 를 `"naver_place"` 로 변경. **여기까지 끝나기 전에 교재를 인쇄하지 않는다.**
3. **`verify.py` 재실행** — 2번이 `[ ? ]` → `[ O ]` 로, 5번이 실데이터로도 통과하는지 확인.
4. **`make_snapshots.py` 재실행** — 데이터가 바뀌었으므로 Step별 완성본을 다시 만든다.
5. (선택) **Leaflet 번들 배치** — 없어도 SVG로 동작한다. 넣으면 진짜 지도 타일이 뜬다.

**판단이 필요한 열린 항목 1건**
§4.3의 카테고리 재배분에서 어떤 역사문화 장소 3곳을 뺄지는 교사 재량이다.
사찰 4곳(마곡사·갑사·동학사·신원사) 중 일부를 빼면 분포는 좋아지지만 **거리 점수의 다양성이 줄어든다**
— 원도심 밖 장소가 있어야 §5.3의 기준점 변경 실습과 §5.6의 차량 구간이 눈에 보인다.
권장: 사찰은 3곳 유지, 박물관·미술관 계열에서 성격이 겹치는 2곳과 사찰 1곳을 뺀다.

---

## 17. v2.0 → v2.1 변경 이력 (구현하며 드러난 것)

기획만으로는 보이지 않다가, 코드를 실제로 돌려 보고서야 드러난 6건이다.

| # | 발견 | 조치 | 어디서 드러났나 |
|---|---|---|---|
| **1** | **윈도우 콘솔이 cp949라 한글 print에서 스크립트가 죽는다.** `—`(em dash) 한 글자에 `UnicodeEncodeError`로 멈춘다 | `scripts/console_utf8.py` 신규, 전 스크립트가 첫 줄에서 import · §14 리스크 추가 | 시드 생성 스크립트 첫 실행 |
| **2** | **카테고리 매핑은 원문 단위**라 원문이 같으면 분류도 같아진다. 부록 A의 장소별 배정을 그대로 실현할 수 없다 | §4.3에 제약 명시 + `category_raw` 세분 수집 지침 | 매핑표를 실제로 채우다 |
| **3** | **순위 뒤집힘은 저절로 생기지 않는다.** 보정 전 랭킹도 리뷰수로 동점을 깨므로 순서가 거의 같다. 첫 시드에서는 뒤집힘이 2곳뿐이라 §10-5 실패 | §4.6 신규 — 시드 분포 조건(저리뷰·고별점 4곳 이상)을 A2 요구사항으로 승격 | `verify.py` 5번 실패 |
| **4** | 지도 그림을 홈·동선·오프라인 **세 화면이 공유**한다. 같은 SVG를 세 번 적으면 한 곳만 고치는 사고가 난다 | `templates/_svgmap.html` 매크로 신규. Jinja는 `with context` 없이 import하면 전역값이 안 보인다는 함정도 포함 | 범례가 빈 채로 렌더됨 |
| **5** | 전체 지도에 축척 여유 1.6배를 주면 **여백만 넓어지고 원도심이 뭉친다.** 1.6배는 번호·이름이 붙는 동선 그림용 값이다 | `geo.project_points(zoom_out=)` 인자화. 전체 보기는 1.05 | 첫 화면 육안 확인 |
| **6** | 검증 결과가 PASS/FAIL 두 가지뿐이면, **"A2가 아직"인 상태와 "코드가 틀렸다"가 구별되지 않는다** | `PENDING` 상태 추가 (§10) | 분류별 4곳 검사 |

부수적으로 확정한 것

- `verify.py` 12번의 `subprocess`는 윈도우에서 `encoding="utf-8"` 을 지정해야 한다 (1번과 같은 뿌리)
- 검증용 라우트 점검은 `test_client()` 마다 새 게스트 사용자를 만든다 — 학생 데이터를 건드리지 않는다
- 확장 과제(§13)는 **기본값 꺼짐**으로 구현했다. 아무 인자도 주지 않으면 기본 과제와 완전히 같게 동작하며, `verify.py` 가 회귀로 보증한다

## 부록 A. 시드 30곳 — 카테고리 배정 초안

v2.4 부록 A의 공주시 30곳(전부 네이버 지역검색으로 실재 확인)에 표준 6분류를 배정한 **초안**이다.
`category_raw`(네이버 원문)와 별점·리뷰수는 A2에서 수집해 채운다.
⚠ 표시는 §4.3의 분포 조정 대상이다.

| # | 장소 | 표준 분류 | # | 장소 | 표준 분류 |
|---|---|---|---|---|---|
| 1 | 공산성 | 역사문화 | 16 | 공주산성시장문화공원 | 휴식 |
| 2 | 공주무령왕릉과왕릉원 | 역사문화 | 17 | 공주목재문화체험장 | 체험 |
| 3 | 국립공주박물관 | 역사문화 | 18 | 대장이랜드중장농촌체험휴양마을 | 체험 |
| 4 | 석장리박물관 | 역사문화 | 19 | 계룡산상신농촌체험휴양마을 | 체험 |
| 5 | 충청남도역사박물관 | 역사문화 ⚠ | 20 | 아트센터고마 | 체험 |
| 6 | 마곡사 | 역사문화 | 21 | 임립미술관 | 휴식 |
| 7 | 갑사 | 역사문화 | 22 | 이미정갤러리 | 휴식 ⚠ |
| 8 | 동학사 | 역사문화 | 23 | 대통길작은미술관 | 체험 |
| 9 | 신원사 | 역사문화 ⚠ | 24 | 자연미술관Ko | 자연 |
| 10 | 천주교황새바위순교성지 | 역사문화 | 25 | 공주산성시장 | 먹거리 |
| 11 | 공주한옥마을 | 역사문화 | 26 | 개성집공주점 | 먹거리 |
| 12 | 유구색동수국정원 | 자연 | 27 | 고마나루1999 | 먹거리 |
| 13 | 계룡저수지 | 자연 | 28 | 공산성본가 | 먹거리 |
| 14 | 고마나루솔밭 | 자연 | 29 | 베이커리밤마을 | 카페감성 |
| 15 | 공주산림휴양마을 | 자연 | 30 | 곡물집 | 카페감성 |

**초안 집계: 역사문화 11 · 자연 5 · 체험 5 · 먹거리 4 · 카페감성 2 · 휴식 3**
→ A2에서 **카페 3곳, 공원·쉼터 1곳을 추가**하고 ⚠ 3곳을 제외해
**역사문화 8 · 자연 5 · 체험 5 · 먹거리 4 · 카페감성 5 · 휴식 3 = 30곳**으로 맞춘다.
(휴식이 4곳에 못 미치면 공원·쉼터를 1곳 더 추가하고 역사문화에서 1곳을 더 뺀다.)

---

## 부록 B. `places_raw.csv` 컬럼 스펙 (33행)

| 컬럼 | 타입 | 비고 |
|---|---|---|
| `name` | 문자열 | 중복 2건 포함. 지점 접미사 표기 흔들림 |
| `category_raw` | 문자열 | 네이버 원문 그대로. 33행 전부 미매핑 상태 |
| `road_address` | 문자열 | 도로명주소 |
| `mapx` / `mapy` | 정수 | 10^7로 나누면 경도/위도. **1행 결측** |
| `star` | 실수 | **5행 결측(빈칸)** |
| `visitor_rev` | 문자열 | **6행이 콤마 표기(`"1,204"`)**, 나머지는 정수 문자열 |
| `blog_rev` | 문자열 | 위와 동일 규칙 |
| `collected_at` | 문자열 | `2026-08` — 전 행 동일 |

**읽기 규칙: 전 CSV `encoding='utf-8-sig'` 고정.**
학생 30명 중 몇 명은 반드시 CSV를 Excel로 열어 저장하고, 그 순간 한글이 깨진다.
Step 1 프롬프트에 인코딩을 못박아 둔 이유다.
